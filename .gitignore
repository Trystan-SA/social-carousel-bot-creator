node_modules/
npm-debug.log*
.DS_Store

# Rendered output
**/output/

# Personal content — commit nothing here by default
posts/*
!posts/.gitkeep
assets/*
!assets/.gitkeep

# Personal playbooks
strategy.md
CLAUDE.local.md
# Ignore Claude Code local state, but keep the repo's own skills folder.
/.claude/*
!/.claude/skills/
# Inside templates/, .claude/ ships verbatim so users get the skills.
# (templates/ is not under .claude/, so nothing to re-include here.)

# Editor / tooling
.vscode/
.idea/
*.swp
